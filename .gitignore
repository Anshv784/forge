# Rust
/target
**/target
Cargo.lock.bak

# Anchor
.anchor/
test-ledger/
.surfpool/

# Third-party source checked out locally for the ZeroClaw integration spike —
# never vendored into this repo
.zeroclaw-src/

# Real ZeroClaw instance's runtime state for the demo — keep config.toml and
# the skill (both committed explicitly below); everything else is either a
# copy of plugins/bundle/ (installed via `zeroclaw plugin install`) or
# ephemeral runtime data.
demo/.zeroclaw-config/plugins/
demo/.zeroclaw-config/data/
demo/.zeroclaw-config/state/

# Node / Next.js
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
npm-debug.log*
tests-dist/

# Env / secrets
.env
.env.local
.env.*.local
*.pem
*.pkcs8
*keypair*.json
!spikes/**/fixtures/*keypair*.json
.secrets/

# OS
.DS_Store

# WASM build artifacts
*.wasm
!plugins/bundle/**/*.wasm

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
